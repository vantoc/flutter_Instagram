import 'package:flutter/material.dart';

class WebSceenLayout extends StatelessWidget {
  const WebSceenLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('This is Web'),
    );
  }
}
